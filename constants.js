const Discord = require('discord.js');

const PERM_FLAGS = Discord.Permissions.FLAGS;
const INTENT_FLAGS = Discord.Intents.FLAGS;

module.exports = {permFlags: PERM_FLAGS, intentFlags: INTENT_FLAGS};
